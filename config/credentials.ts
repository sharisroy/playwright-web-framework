// Test account credentials for the practice site.
// Note: these are throwaway credentials for a public practice site. For real apps,
// keep secrets out of the repo (e.g. read from process.env via a .env file).
export const credentials = {
    username: 'practiceuser1',
    password: 'PracticePass1!',
}
